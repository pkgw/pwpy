#!/bin/csh

if ($#argv < 1) then
	echo "Must give a cal-dataset"
	exit(0)
endif

set vis=$1
set line=chan,1,600,200
shift

uvplt nxy=3,2 yrange=-180,180 vis=$vis device=/xs select="pol(xx,yy),-auto" axis=time,pha yrange=-180,180 options=2pass,source line=$line  $*

exit 0
